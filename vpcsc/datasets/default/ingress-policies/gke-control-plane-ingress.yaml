# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# Ingress: GKE control plane communication
#
# Compliance:
#   ISO 27001: A.8.22 Segregation — managed exception for platform service control plane traffic
#   GDPR: Art. 28(1) Processor obligations — authorized managed service provider access
#   NIS2 Directive: Art. 21(2)(a) Risk analysis — managed service exception for control plane
#
# GKE control plane runs outside the perimeter in Google-managed infra.
# Without this rule, kubectl and GKE API calls fail.
#
# Project numbers are resolved at terraform plan time via FAST's
# $project_numbers:<key> interpolation, keyed by project-factory project name.
#
# ARCHITECT TODO:
# - [ ] Verify all GKE environment projects are listed

from:
  identities:
  - serviceAccount:service-$project_numbers:gke-dev-0@container-engine-robot.iam.gserviceaccount.com
  - serviceAccount:service-$project_numbers:gke-stg-0@container-engine-robot.iam.gserviceaccount.com
  - serviceAccount:service-$project_numbers:gke-prd-0@container-engine-robot.iam.gserviceaccount.com
  - serviceAccount:service-$project_numbers:gke-int-0@container-engine-robot.iam.gserviceaccount.com
  sources:
  - access_level: '*'
to:
  operations:
  - method_selectors:
    - '*'
    service_name: container.googleapis.com
  - method_selectors:
    - '*'
    service_name: compute.googleapis.com
  - method_selectors:
    - '*'
    service_name: artifactregistry.googleapis.com
  resources:
  - $project_ids:acme-corp-gke-dev-0
  - $project_ids:acme-corp-gke-stg-0
  - $project_ids:acme-corp-gke-prd-0
  - $project_ids:acme-corp-gke-int-0
